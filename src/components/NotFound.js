import React, { Component } from 'react';

const NotFound = React.createClass({
  render: function() {
    return (
      <div>
       404 NotFound
      </div>
    );
  }
});

export default NotFound;